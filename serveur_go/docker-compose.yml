version: '3.8'

services:
  esp32-dashboard:
    build: .
    container_name: esp32-dashboard
    restart: unless-stopped
    expose:
      - 8081 
    volumes:
      - ./credentials.json:/app/credentials.json:ro